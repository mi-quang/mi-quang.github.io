
<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ page.title }}</title>
    <link rel="stylesheet" href={{ "/assets/css/styles.css" |  relative_url }}>
    <link rel="icon" type="image/x-icon" href={{ site.icon |  relative_url }}>
  </head>
  <body>
    {% include navigation.liquid %}
      {{ content }}
  

    <!-- Java Script -->
  {% include scripts/mathjax.liquid %} 
  </body>
  
 
</html>